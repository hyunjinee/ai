[project]
name = "ai"
version = "0.1.0"
description = "AI projects workspace"
dependencies = []

[tool.uv]
dev-dependencies = [
    "pytest>=8.0.0",
    "black>=24.0.0",
    "ruff>=0.5.0",
    "mypy>=1.0.0",
    "ipykernel>=6.29.0",
]

[tool.uv.workspace]
members = [
    "packages/*",
]
exclude = [
    "packages/airflow",
] 