# LangGraph TypeScript Examples

LangGraph를 사용한 TypeScript 예제 프로젝트

## 설치

```bash
pnpm install
```

## 실행

```bash
# 개발 모드 (watch mode)
pnpm dev

# 일반 실행
pnpm start

# 빌드
pnpm build
```

## 환경 변수

`.env` 파일을 생성하고 필요한 API 키를 설정:

```
OPENAI_API_KEY=your_api_key_here
```

