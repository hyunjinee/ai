[project]
name = "handsonllm"
version = "0.1.0"
description = "Hands-on LLM 학습 프로젝트"
requires-python = ">=3.12"
dependencies = [
    "transformers>=4.47.0",
    "torch>=2.0.0",
    "accelerate>=1.0.0",
]

[tool.uv]
package = false
