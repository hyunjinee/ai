[project]
name = "crawling"
version = "0.1.0"
description = "Web crawling and scraping utilities"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
    "beautifulsoup4>=4.12.0",
    "selenium>=4.15.0",
    "lxml>=5.0.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0.0",
]

